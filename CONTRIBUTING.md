# Contributing

感谢参与 Your Turn。

1. 先在 Issue 中说明问题、用户场景与预期行为。
2. Fork 仓库并从 `main` 创建分支。
3. 不要提交密钥、个人路径或真实用户数据。
4. 运行 `pnpm run verify` 与 `git diff --check`。
5. Pull Request 中说明改动和验证结果。

项目不展示 chain-of-thought。新界面只应呈现显式计划、动作、证据、产物和用户决策。
